[project]
name = "lol"
version = "0.1.0"
description = "撸了吗 - 一个支持多媒体的打卡系统"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.109.0",
    "uvicorn[standard]>=0.27.0",
    "aiofiles>=23.2.1",
    "python-multipart>=0.0.6"
]
